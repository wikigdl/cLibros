<% if user_signed_in? %>
    <li>
      <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
    </li>
<% else %>
    <li>
      <%= form_for("user", :url => user_session_path) do |f| %>
          <%= f.text_field :email,:title => 'expediente@iteso.mx', :value=>'email',:style => "width: 190px;margin-left:5px;" %>
          <%= f.password_field :password, :title=> 'Contraseña',:style => "width: 190px;margin:5px;" %>
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
          <%= f.submit 'Sign in' %>
          <%= link_to "Forgot your password?", new_password_path('user') %>
      <% end %>
    </li>
<% end %>
